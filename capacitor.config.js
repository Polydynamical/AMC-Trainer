import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'amc-vue-ionic',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
